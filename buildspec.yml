version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd frontend
      - npm install
  build:
    commands:
      - npm install
    finally:
      - echo This is the finally block execution!
artifacts:
  base-directory: frontend/dist
  files:
    - '**/*'