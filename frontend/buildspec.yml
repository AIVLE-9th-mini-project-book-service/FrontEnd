version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd $CODEBUILD_SRC_DIR/frontend
      - npm install
  build:
    commands:
      - cd $CODEBUILD_SRC_DIR/frontend
      - npm run build
artifacts:
  files:
    - '**/*'
  base-directory: frontend/dist